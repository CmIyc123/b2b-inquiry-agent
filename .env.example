# Local API
HOST=127.0.0.1
PORT=3000
ADMIN_API_KEY=replace-with-a-long-random-value

# Optional browser frontend origin when the UI is hosted separately
FRONTEND_ORIGIN=

# Meta WhatsApp Business Cloud API
# Keep the real values in a local .env or deployment secret store.
META_APP_ID=
META_APP_SECRET=
WHATSAPP_VERIFY_TOKEN=
WHATSAPP_ACCESS_TOKEN=
WHATSAPP_PHONE_NUMBER_ID=
